/*
	Header file for funtions used in trace_handler.c
	Author: Srikar
*/

// Function prototypes
int get_num_traces(FILE *tracefile);
// trace_entry* get_traces(FILE *tracefile);
// trace_entry* new_trace_array(int size);